azure-functions>=1.21.0
azure-identity>=1.19.0
azure-storage-blob>=12.24.0
pyodbc>=5.2.0

# Pre-processing quality checks (quality_check.py).
# All are optional at runtime: each format-specific check skips with an INFO
# issue if the library is missing, but installing them gives full coverage.
pymupdf>=1.24            # PDF page/text/image inspection
pillow>=10.0             # Image decode, EXIF, blur/contrast heuristics
python-docx>=1.1         # .docx text + structure
openpyxl>=3.1            # .xlsx workbook + sheet inspection
python-pptx>=0.6         # .pptx slide inspection
